data:
  # Fallback path if DATA_CSV env var is not set
  csv_path: "data/synthetic_fb_ads_undergarments.csv"
  date_column: "date"

windows:
  baseline_days: 14      # e.g. first 14 days in range
  comparison_days: 14    # e.g. last 14 days in range

thresholds:
  min_impressions: 500000   # for cluster-level insights
  min_spend: 1000.0
  min_purchases: 50

random:
  seed: 42
